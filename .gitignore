node_modules/
dist/
*.log
.DS_Store
.env

# Built client bundle (generated by `bun run build`)
src/client/app.js

# Local TLS certs
certs/

# Editor / tooling
.claude/
.obsidian/

# Runtime data — your ideas live here but are never committed.
# .gitkeep is tracked so the directory exists on a fresh clone.
/ideas/*
!/ideas/.gitkeep

# Google Drive sync conflict copies (e.g. "README 2.md", "src 2/")
* 2.*
* 3.*
* 2
* 3
